sed -i -r 's:"(/system):"/org/gnome\1:g' schemas/*.in          &&
./configure --prefix=${PROJECT_PREFIX}                         &&
sudo make -j 32                                                     &&
sudo make install
